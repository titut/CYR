# Core dependencies for the semantic navigation library.

# Built-in modules (no install needed):
#   tkinter, json, dataclasses, pathlib, typing

# Map editor and simulator
numpy>=1.24.0
openai>=1.0.0
pygame>=2.5.0
shapely>=2.0.0

# Zenoh pub/sub transport used by all nodes.
eclipse-zenoh==1.9.0
